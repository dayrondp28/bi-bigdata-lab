name: CI - Validate database changes

on:
  pull_request:
    branches:
      - main

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Liquibase
        uses: liquibase/setup-liquibase@v3.0.0
        with:
          version: '4.32.0'
          edition: 'community'

      - name: Download Liquibase Databricks Extension
        run: |
          curl -sL "https://repo1.maven.org/maven2/org/liquibase/ext/liquibase-databricks/4.32.0/liquibase-databricks-4.32.0.jar" -o lib/liquibase-databricks.jar

      - name: Validate changelog
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_HTTP_PATH: ${{ secrets.DATABRICKS_HTTP_PATH }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          DATABRICKS_CATALOG: ${{ secrets.DATABRICKS_CATALOG }}
          LIQUIBASE_CLASSPATH: ${{ github.workspace }}/lib/databricks-jdbc.jar:${{ github.workspace }}/lib/liquibase-databricks.jar
        run: |
          liquibase \
            --changelog-file=changelog/db.changelog-master.sql \
            --driver=com.databricks.client.jdbc.Driver \
            --url="jdbc:databricks://${DATABRICKS_HOST}:443/default;AuthMech=3;httpPath=${DATABRICKS_HTTP_PATH};ConnCatalog=${DATABRICKS_CATALOG}" \
            --username=token \
            --password="${DATABRICKS_TOKEN}" \
            --default-catalog-name="${DATABRICKS_CATALOG}" \
            --default-schema-name="default" \
            --database-changelog-table-name="databasechangelog" \
            --database-changelog-lock-table-name="databasechangeloglock" \
            validate

      - name: Show pending changesets
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_HTTP_PATH: ${{ secrets.DATABRICKS_HTTP_PATH }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          DATABRICKS_CATALOG: ${{ secrets.DATABRICKS_CATALOG }}
          LIQUIBASE_CLASSPATH: ${{ github.workspace }}/lib/databricks-jdbc.jar:${{ github.workspace }}/lib/liquibase-databricks.jar
        run: |
          liquibase \
            --changelog-file=changelog/db.changelog-master.sql \
            --driver=com.databricks.client.jdbc.Driver \
            --url="jdbc:databricks://${DATABRICKS_HOST}:443/default;AuthMech=3;httpPath=${DATABRICKS_HTTP_PATH};ConnCatalog=${DATABRICKS_CATALOG}" \
            --username=token \
            --password="${DATABRICKS_TOKEN}" \
            --default-catalog-name="${DATABRICKS_CATALOG}" \
            --default-schema-name="default" \
            --database-changelog-table-name="databasechangelog" \
            --database-changelog-lock-table-name="databasechangeloglock" \
            status --verbose

      - name: Preview SQL
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_HTTP_PATH: ${{ secrets.DATABRICKS_HTTP_PATH }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
          DATABRICKS_CATALOG: ${{ secrets.DATABRICKS_CATALOG }}
          LIQUIBASE_CLASSPATH: ${{ github.workspace }}/lib/databricks-jdbc.jar:${{ github.workspace }}/lib/liquibase-databricks.jar
        run: |
          liquibase \
            --changelog-file=changelog/db.changelog-master.sql \
            --driver=com.databricks.client.jdbc.Driver \
            --url="jdbc:databricks://${DATABRICKS_HOST}:443/default;AuthMech=3;httpPath=${DATABRICKS_HTTP_PATH};ConnCatalog=${DATABRICKS_CATALOG}" \
            --username=token \
            --password="${DATABRICKS_TOKEN}" \
            --default-catalog-name="${DATABRICKS_CATALOG}" \
            --default-schema-name="default" \
            --database-changelog-table-name="databasechangelog" \
            --database-changelog-lock-table-name="databasechangeloglock" \
            update-sql